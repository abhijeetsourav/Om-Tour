GROQ_API_KEY=
NEXT_PUBLIC_CPK_PUBLIC_API_KEY=
NEXT_PUBLIC_COPILOTKIT_RUNTIME_URL=http://localhost:8000/copilotkit

# Optional: use a deployed LangGraph agent instead of the local backend.
NEXT_PUBLIC_LGC_DEPLOYMENT_URL=
LGC_DEPLOYMENT_URL=

# Optional: only needed for the deployed LangGraph platform endpoint path.
LANGCHAIN_API_KEY=